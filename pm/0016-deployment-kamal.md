# 0010 — Deployment (Kamal 2)

Goal: ship to a single VM. Kamal config, Thruster, Let's Encrypt, Litestream backups of all four SQLite files to S3-compatible bucket. Smoke test from a fresh browser.

Detail at milestone start (need host + domain decisions first).
