# 0004 — Holded integration

Goal: thin Holded API client (plain `Net::HTTP`), used by jobs to push expenses, fetch bank transactions, send invoices through VeriFactu. Credentials via encrypted Rails credentials.

Detail at milestone start.
