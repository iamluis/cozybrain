# 0006 — Folder sync (gestoría view)

Goal: a service that mirrors `IncomingDocument` + `IssuedInvoice` into the on-disk folder tree described in `brain.md` §gestoría interface. Idempotent. Either a local dir synced by Dropbox/iCloud, or rclone to S3-compatible — TBD with Luis.

Detail at milestone start.
